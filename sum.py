def calculate_sum(a=10, b=20):
    """
    Calculate the sum of two numbers.
    
    Args:
        a (int): First number, defaults to 10
        b (int): Second number, defaults to 20
        
    Returns:
        int: The sum of a and b
    """
    result = a + b
    print(result)
    return result
